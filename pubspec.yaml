name: biosync
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  go_router: ^17.0.0
  url_launcher: ^6.2.0

  # --- FIREBASE ATUALIZADO PARA WINDOWS ---
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  _flutterfire_internals: ^1.3.64
  firebase_auth_platform_interface: ^8.1.4
  cloud_firestore_platform_interface: ^7.0.4
  firebase_core_web: ^3.3.0
  firebase_auth_web: ^6.1.0
  cloud_firestore_web: ^5.1.0
  # ----------------------------------------

  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/
